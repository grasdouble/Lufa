# AI Instructions: Router (Apps)

## Context

You are working on the **Apps** domain (Microfrontends).

## 1. Task Routing

Currently, specific instruction files are **pending**.

- Use best practices for **React** and **Vite**.
- Follow the patterns established in `packages/design-system` where applicable (strict typing, modularity).

## 2. Global Context Awareness

- **Monorepo**: Remember you are in a monorepo.
- **Design System**: Use `@grasdouble/lufa_design-system` for UI components. DO NOT create local UI components if they belong in the Design System.
