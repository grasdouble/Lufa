---
sidebar_position: 3
---

import { LiveDemo, Variants, Examples } from "../../../src/dsExamples/display/avatarGroup";
import { AvatarGroup } from "@grasdouble/lufa_design-system";

# AvatarGroup

Displays multiple avatars with overlap and an optional “+N” count when the list is truncated.

## Overview

Use `AvatarGroup` to represent a set of people (team members, assignees, participants).

- Overlapped layout for compact representation
- Apply a shared `size` to all children
- Limit displayed avatars with `max` and show a trailing count avatar

## Live Demo

<LiveDemo />

## Installation

```tsx
import { AvatarGroup, Avatar } from "@grasdouble/lufa_design-system";
```

## Basic Usage

```tsx
<AvatarGroup max={3} size="md">
  <Avatar src="/a.jpg" alt="Alice" />
  <Avatar src="/b.jpg" alt="Bob" />
  <Avatar src="/c.jpg" alt="Chris" />
  <Avatar src="/d.jpg" alt="Denise" />
</AvatarGroup>
```

## Props

| Prop      | Type                                   | Default | Description                                       |
| --------- | -------------------------------------- | ------- | ------------------------------------------------- |
| `children`| `ReactNode`                             | -       | Avatars to render                                 |
| `max`     | `number`                                | -       | Maximum avatars displayed before showing `+N`      |
| `size`    | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl'`  | `'md'`  | Applies a size to all child avatars (if unset)     |

## Variants

<Variants />

## Examples

<Examples />

