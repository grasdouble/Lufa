---
sidebar_position: 2
---

import { AutoComplete } from '@grasdouble/lufa_design-system';

import {
  CustomFilter,
  DisabledOptions,
  Disabled,
  ErrorState,
  FullWidth,
  LiveDemo,
  Required,
  Size,
  Variant,
  WithAdornment,
  WithCallback,
  WithHelperText,
} from '../../../src/dsExamples/forms/autocomplete';

# AutoComplete

An autocomplete input component that provides dynamic suggestions as users type, with keyboard navigation and custom filtering support.

## Overview

Use `AutoComplete` when you need…

- Help users find options from a large list quickly
- Search functionality with real-time suggestions
- Keyboard navigation through filtered options
- Custom filtering logic for specific use cases
- Support for disabled options

## Live Demo

<LiveDemo />

## Import

```tsx
import { AutoComplete } from '@grasdouble/lufa_design-system';
import type { AutoCompleteOption } from '@grasdouble/lufa_design-system';
```

## Basic Usage

```tsx
const options = [
  { value: 'us', label: 'United States' },
  { value: 'uk', label: 'United Kingdom' },
  { value: 'ca', label: 'Canada' },
];

<AutoComplete
  label="Select Country"
  placeholder="Type to search..."
  options={options}
  onSelect={(option) => console.log(option)}
/>
```

## Props

| Prop            | Type                                                    | Default      | Description                              |
| --------------- | ------------------------------------------------------- | ------------ | ---------------------------------------- |
| `label`         | `string`                                                | -            | Label text above the input               |
| `options`       | `AutoCompleteOption[]`                                  | `[]`         | Array of options to display              |
| `size`          | `'small' \| 'medium' \| 'large'`                        | `'medium'`   | Size of the input                        |
| `variant`       | `'outlined' \| 'filled'`                                | `'outlined'` | Visual style variant                     |
| `fullWidth`     | `boolean`                                               | `false`      | Makes the input take full container width|
| `onSelect`      | `(option: AutoCompleteOption) => void`                  | -            | Callback when an option is selected      |
| `filterOption`  | `(option: AutoCompleteOption, input: string) => boolean`| -            | Custom filter function                   |
| `error`         | `string`                                                | -            | Error message to display                 |
| `helperText`    | `string`                                                | -            | Helper text below the input              |
| `disabled`      | `boolean`                                               | `false`      | Disables the input                       |
| `required`      | `boolean`                                               | `false`      | Marks the field as required              |
| `startAdornment`| `ReactNode`                                             | -            | Element to display at the start of input |
| `endAdornment`  | `ReactNode`                                             | -            | Element to display at the end of input   |

Also supports standard `input` props (for example `className`, `aria-*`, `onChange`, …).

## Examples

### size

<Size />

### variant

<Variant />

### With Adornment

<WithAdornment />

### Full Width

<FullWidth />

### With Helper Text

<WithHelperText />

### Error State

<ErrorState />

### Disabled

<Disabled />

### Required

<Required />

### With Callback

<WithCallback />

### Custom Filter

<CustomFilter />

### Disabled Options

<DisabledOptions />

## Accessibility

- Uses native input for screen reader support
- Supports keyboard navigation:
  - **Arrow Down/Up**: Navigate through options
  - **Enter**: Select highlighted option
  - **Escape**: Close dropdown
  - **Tab**: Move to next focusable element
- Includes proper ARIA attributes (`role="listbox"`, `aria-selected`)
- Label is associated with the input using `htmlFor`
- Error states are announced to screen readers via `aria-describedby`
- Disabled options are properly marked with `aria-disabled`

## Best Practices

### Do ✅

- Use clear, concise option labels
- Provide helpful placeholder text
- Show the most relevant options first
- Use helper text to guide users
- Implement custom filtering for domain-specific searches
- Mark disabled options appropriately

### Don't ❌

- Don't use for fewer than 5 options (use Select instead)
- Don't disable keyboard navigation
- Avoid using without a label for accessibility
- Don't make option labels too long (truncate if necessary)
- Don't implement without proper filtering logic

## Related Components

- **Input** - For simple text input without suggestions
- **Select** - For smaller, predefined option sets
