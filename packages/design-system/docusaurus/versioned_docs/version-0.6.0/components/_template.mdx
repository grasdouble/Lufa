---
sidebar_position: 99
draft: true
---

import { ComponentName } from '@grasdouble/lufa_design-system';

import {
  CardHeaderExample,
  InlineListExample,
  LiveDemo,
  OverflowCountExample,
  PropNameA,
  PropNameB,
  PropNameC,
} from '../../_examples/{category}/componentName';

# ComponentName

One-sentence summary of what this component is for.

## Overview

Use `ComponentName` when you need…

- Key capability
- Key capability
- Key capability

## Live Demo

<!-- Prefer adding a small title/caption inside each example block (handled in the dsExamples component). -->

<LiveDemo />

## Import

```tsx
import { ComponentName } from '@grasdouble/lufa_design-system';
```

## Basic Usage

```tsx
<ComponentName>Content</ComponentName>
```

## Props

| Prop       | Type                                 | Default     | Description                             |
| ---------- | ------------------------------------ | ----------- | --------------------------------------- |
| `children` | `ReactNode`                          | -           | Content to display inside the component |
| `variant`  | `'primary' \| 'secondary' \| 'text'` | `'primary'` | Visual style variant                    |
| `size`     | `'small' \| 'medium' \| 'large'`     | `'medium'`  | Size of the component                   |
| `disabled` | `boolean`                            | `false`     | Whether the component is disabled       |

## Variants

### PropNameA

<PropNameA />

### PropNameB

<PropNameB />

### PropNameC

<PropNameC />

<!-- Continue with other props as needed... -->

## Best Practices

### Do ✅

- Use `ComponentName` to…
- Ensure to...
- ...

### Don't ❌

- Avoid using `ComponentName` for…
- Don't forget to...
- ...

## Accessibility

<!-- Add component-specific accessibility notes (keyboard, ARIA, focus, semantics). -->

## Examples of usage

<!-- Prefer adding a small title/caption inside each example block (handled in the dsExamples component). -->

<CardHeaderExample />
<InlineListExample />
<OverflowCountExample />

<!-- Continue with other examples as needed... -->
